-- brew services start postgresql (Mac OS only)
-- psql postgres

-- \l -> melihat semua database
-- \c markovkaryawandb -> Connect to the newly created database
-- \dt -> lihat semua relations
-- \q -> exit
-- CREATE DATABASE markovkaryawandb;

-- CREATE TABLE users (
--     id SERIAL PRIMARY KEY,
--     username VARCHAR(255) UNIQUE NOT NULL,
--     password VARCHAR(255) NOT NULL
-- );

-- INSERT INTO users (username, password) 
-- VALUES ('vincent', 'vincent');

-- DROP TABLE users;
